import Vue from 'vue'
// import App from './App.vue'
import Mailform from "./Mailform.vue"

// new Vue({
//   el: '#app',
//   render: h => h(App)
// })
new Vue({
  el: '#mailform',
  render: h => h(Mailform)
})
